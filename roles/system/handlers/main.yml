---
- name: Restart Dock
  ansible.builtin.shell: killall Dock || true
  changed_when: false

- name: Restart Finder
  ansible.builtin.shell: killall Finder || true
  changed_when: false

- name: Restart SystemUIServer
  ansible.builtin.shell: killall SystemUIServer || true
  changed_when: false
